name: success
jobs:
  build:
    steps:
      - run: |
          #include local.inc
          #include shared.inc
          #include missing.inc || fallback.inc
          #include empty.inc || fallback-after-empty.inc
          #include unreadable.inc || fallback-after-unreadable.inc
          #include optional.inc || null
          #include recursive.inc
          #include templated.inc animal = "otter" adjective = "sleek"
